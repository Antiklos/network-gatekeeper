- Change the firewall to a blacklist rather than a whitelist, so there is no race condition for the first kb or so of data being blocked, and so the request message can be removed.
- Create a new payment class to implement sending and receiving bitcoin or possibly some other crypto
- Make contract data size configurable
- Make gated and non-gated interfaces either configurable or automatic
- Refactor to remove all the unnecessary string manipulation
- Refactor state array to make a linked list instead
- Implement tests
- Move the cli_socket and payment message to a unix socket instead of inet?
- Smarter default client behavior for polling all interfaces and choosing the best deal
- Implement or interface with a reputation system to prevent abuse
- GUI for config
